<UserControl x:Class="MVVM.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MVVM.ViewModels"
             xmlns:local="clr-namespace:MVVM.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:LoginViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBox Width="150" Height="25" Text="{Binding Login}"/>
            <TextBox Width="150" Height="25" Margin="0 5 0 0" Text="{Binding Password}" />
        </StackPanel>
        
        <Button Grid.Row="1" Grid.Column="0" Content="Add" Width="75" Height="20" Margin="0 5 0 0" Command="{Binding LoginCommand}" CommandParameter="{Binding Login}"/>
        <ListBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Users}" DisplayMemberPath="Display"/>
    </Grid>
</UserControl>