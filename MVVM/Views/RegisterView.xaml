<UserControl x:Class="MVVM.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MVVM.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock Text="RegisterView" HorizontalAlignment="Center"/>
                <TextBox Width="150" Height="25" Text="{Binding Login}"/>
                <TextBox Width="150" Height="25" Margin="0 5 0 0" Text="{Binding Password}"/>
                <Button Grid.Row="0" Grid.Column="0" Content="Register" Width="75" Height="20" Margin="0 5 0 0" Command="{Binding RegisterCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
